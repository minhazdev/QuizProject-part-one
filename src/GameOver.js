const GameOver = ({resetQuiz}) => {
    
 return(
      <div>
            Total Score 7
            GameOver
            <button onClick={resetQuiz}>Reset Quiz</button>
      </div>
    )

}

export default GameOver